@PACKAGE_INIT@

include("${CMAKE_CURRENT_LIST_DIR}/eggs.test-targets.cmake")

# Makes EggsTest.cmake (eggs_discover_tests()) available via include(EggsTest)
list(APPEND CMAKE_MODULE_PATH "${CMAKE_CURRENT_LIST_DIR}")

check_required_components(Eggs.Test)
check_required_components(Eggs.TestMain)
